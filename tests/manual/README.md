# Manual Test Scripts

This folder contains manual scripts and demos that are not part of the default pytest suite.

## How to run
Execute the scripts directly with Python, for example:
- `python tests/manual/quick_test.py`
- `python tests/manual/ollama_server_smoke.py --model <model>`
- `python tests/manual/demo_debug_mode.py`
